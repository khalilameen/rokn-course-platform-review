<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Shipping builds trust the system certificate store and reject HTTP. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
